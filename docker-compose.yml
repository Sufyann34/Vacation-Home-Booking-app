services:
  listing_service:
    build: ./listing_service
    container_name: listing_service
    ports:
      - "9090:80"
    env_file:
      - .env
    volumes:
      - ./listing_service/app:/listing_service/app

volumes:
  data:
    driver: local
